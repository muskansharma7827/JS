the content in the code, how it looked in the browser will be taken care by html(structure), the formatting, styling by css, and the action take by teh button, or more tags will be taken care by javascript.
it is scripting lang,lightweight lang,there I person came and write c++ program and add js also, so, this combination is called Node which helped in backend.
this js display in  browser, client-side.also, backend, server-side also.
what is ECMA - it is the standardisation of javascript, that nobody can write their own syntax basically ECMA is also a script, so, to follow the specification,standardisation, a language is made that is called javascript, using ECMA or more languages made.
earlier, js called Mocha, then LiveScript, and finally JavaScript.
in c++, ther needs a comper which make the code, machine understandable, so, in same js also , needs js engine, ther are js engines avaliable like-
V8,SpiderMonkey,JavaScriptCore,Chakra,etc.
JS code can be run in browser as well as vscode means nodejs should be installed, go to terminal,node filename . how ?
browser like u can run in any browser, inspect, go to console, and write snyntax, u wnat to print ajd also can answer mathematical operations.
js code should be write in script tag, under haed tag,body tag, starting and ending, but the best practice to write jscode in at last of body tag, but ther may be chances that in script tag, teh code qwill be refertr to the code thgat is in html,css ofcourse, so, write at last.
the o/p writeen in html code, at last of body tag,<script>
    console.log("hello world");
</script>
the o/p will be Go Live do inspect, console, refresh teh page and then u will be see, hello world.
but u can also, write it create otherfile, like style.css, the best practice,instaed of inline js code, make other file and link it.in body tag by <script src="lecture1.js">
</script>
